[package]
name = "skill-fence"
version = "0.1.0"
edition = "2024"
description = "Evidence-backed, local-first release gates for AI agent skills"
license = "Apache-2.0"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3.15"
